~~ Variable Naming Ch.2 ~~

6.1
    nodes - foundValueNodeList
    //В качестве результата возвращает список узлов(нодов), в котором находится одно и то же значение.
    
    price - productPrice
    //Стоимость товара
    
    gasSum - gasVolumeInSI
    //Объём газа в кубометре(В системе СИ).
 
    accel - carAcceleration
    //Ускорение автомобиля
    
    greets - greetingsText
    //Текст приветствия.

6.2
    hash
    //Хэш строки.
    
    factory
    //Паттерн проектирования Фабричный Метод.
    
    encoding
    //Шифрование(кодирование).
    
    identityMatrix
    //Единичная матрица.

6.3
    healthPoint
    //Очки здоровья игрока, атрибут в классе Player.
    
    movementSpeed
    //скорость передвижения игрока, атрибут в классе Player.
    
    damage
    //Урон оружия, атрибут в классе Weapon.

6.4
    head - headNode
    //Головной узел.
    
    tail - tailNode
    //Хвостовой узел.
    
    union - unitedSet
    //В качестве результата возвращает объединённое множество.
    
    r - nodeList
    //В качестве результата возвращает список узлов(нодов).
    
    randNum - randomNum
    //Случайное число.